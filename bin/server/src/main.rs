//! octo service entry point. Composes the REST API and the deposit ingest worker.
//!
//! Wired in Step 11 of the project plan.
#![forbid(unsafe_code)]

fn main() {
    // Logging (tracing-subscriber) and the API + ingest composition are wired in Step 11.
    println!("octo-server scaffold — API and ingest wired in Step 11");
}
